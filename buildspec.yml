version: 0.2

phases:
  pre_build:
    commands:
      - echo Build build on `date`
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
    files: imagedefinitions.json